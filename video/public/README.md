# Footage

Remotion serves this folder to the composition; `staticFile('x.mp4')` reads
from here.

Put the cinematic excerpt here and name it in `CINEMATIC.src` in
`src/edit.ts`. It is gitignored — it is not ours to commit — so this file
exists to keep the folder in the repo for everyone who clones it.
